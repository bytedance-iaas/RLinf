# SO101 工具 Runbook —— 每个脚本干什么、要解决什么问题

本次会话产出并实际运行过的全部 77 个脚本，都在 `tools_so101_session/`。

**每一条都标注了"要解决的问题"**——因为这些脚本几乎都不是设计出来的，而是**某个具体故障逼出来的**。知道它为什么存在，才知道什么时候该用它、什么时候不该。

**通用前置**：`cd /data08/henryg/pai/RLinf`，用 `.venv/bin/python`，导出 Vulkan ICD、`MUJOCO_GL=egl`、`HF_LEROBOT_HOME`、`REPO_PATH`/`PYTHONPATH`。成套流水线脚本自己在开头导出，可直接 `setsid nohup bash <脚本> &`。

---

## 1. 环境搭建与标定

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `so101_smoke.py` | 建环境、reset、走几步随机动作，打印观测形状与关节维度 | 改完任务文件后，**最便宜的"能不能跑"检查**。形状不对或崩溃就不必往下走，省掉在训练里才发现的几小时 |
| `render_so101.py` | 渲染前视/腕视存 png | 场景几何只能用眼睛核对。与真机照片并排看是唯一可靠的判据 |
| `render_scene_grid.py` | 一次渲染 4 个不同种子的前视，拼成网格 | 单张图看不出**生成区分布**对不对。网格能一眼看出方块是否落在该落的范围 |
| `render_ready.py` | 渲染"抬臂就绪位姿"下的前视+腕视 | 复位姿态下的画面不代表策略工作时看到的画面。**腕部相机指向错误就是这样暴露的** |
| `gen_pickonly.py` | 只做"抓起来"的简化规划器探针 | 怀疑任务**物理上做不到**时的最小验证。历史教训：一次 12 小时的 RL 追的是物理上不可能的任务（夹爪闭合下限 8.1 cm > 方块 2.9 cm），30 分钟的探针就能挡住 |
| `replay_demo.py` | 把真机某一集的动作在仿真里重放 | 关节符号/偏置的标定**没有解析解**，只能重放后逐帧对比真机视频 |
| `verify_calib.py` | 真机 vs 仿真的前视+腕视并排对比图 | 每次改标定参数后都要复核；靠记忆判断"看起来对了"会出错 |
| `calib_test.py` / `calib_wflex.py` | 关节符号/偏置扫描；腕部俯仰专项 | 夹爪指向不对时，逐个关节试比整体猜快 |
| `wrist_sweep.py` / `wrist_remount.py` | 腕部相机位姿扫描 / 重新安装 | **相机装在机械臂链节上**，改了关节偏置它会跟着转——每次标定后必须重新指向 |
| `diag_spawn.sh` | 记录方块生成位置与成败的对应（CSV） | 只知道"成功率 X%"没法改进；需要知道**失败集中在哪个区域** |
| `measure_chunk_motion.py` | 测相邻动作块之间图像的变化量 | 回答"动作块是不是太短、相邻块看到的画面几乎没动" |

> **本组最大的教训**：改了环境事实（复位姿态、物体位置）之后，**先重跑规划器探针**。设成真实复位姿态那次，规划器的隐含前提（张开的夹爪、腕滚 π/2）被破坏，成功率从 45/64 掉到 1/60，而报错信息完全指向别处。

---

## 2. 示范生成

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `gen_so101_demos.py` | **主力规划器**：抓取→抬起→搬运→入盒→回位，成功才留 | RL 需要非零起点，而真机数据在仿真里是 0。这是唯一的数据来源 |
| `gen_so101_demos_v2.py` | 同上，放置阶段改用局部细网格闭环修正 | **已验收：无效，别再试**。A/B 显示 20/24 vs 20/24 持平，落点误差中位反而变差。误差产生在**松爪之后**（脱手动力学），不是 IK 精度 |
| `gen_v8_legacy.sh` | 8 worker 并行生成 6×8 cm 框内示范 | 全板 426 cm² 要 2200 条才够密度；先收窄区域，用同样数据量换密度 |
| `v10_gen.sh` | 只在**环形带**（新增区域）生成，四条边带按面积配额 | 扩域时重新生成整个区域是浪费；只补新增部分 |
| `planner_ab.sh` | 规划器新旧版本的对照实验（同区域同种子） | 改了规划器就必须验收。**没有对照的"改进"等于没改** |

> **必守的密度律**：BC 的地板由**示范间距** = √(生成面积 ÷ 示范条数) 决定，不是总量。实测 1.01 cm→12.5%、0.44 cm→56.7%。**扩大生成区域时，示范条数必须按面积同比例加**。

---

## 3. 数据转换

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `convert_v10_demos.py` | **最新版**：复制上一版数据集再**追加**新集，不重编码旧集 | 视频编码占转换 90% 时间（3.3 集/分）。重编码已有的 672 集要 2.75 h，产出**逐字节相同**的文件 |
| `convert_v9_demos.py` | 从零重建：规划器 h5 + 策略 npz 混合 | 需要全新数据集时用。**单位不对称**：npz 的 `state` 已归一化而 `action` 是弧度，h5 两者都是弧度 |
| `convert_v4_demos.py` / `convert_v7_demos.py` / `convert_v8_demos.py` | 各阶段的转换 | 复现历史结果 |
| `convert_demos_to_lerobot.py` | 最早的通用转换器 | 参考用 |
| `convert_pp5_rollouts.py` / `convert_pp6_rollouts.py` / `convert_pp7_rollouts.py` | 早期的策略轨迹转换 | 历史 |
| `merge_mix_v5.py` | 合并两个数据集 | **注意**：早期版本用 cv2 读 AV1 视频会静默失败（得到 0 条），已加计数闸门 |
| `normstats_sub.py` | 计算/替换 norm_stats | **慎用**——见下 |

> **血统冻结**：同一条血统内**绝不重算** norm_stats。A/B 实测：只换统计量、其余全不变，19.5%→9.4%。归一化统计量定义了模型看到的世界，中途更换等于悄悄换了世界。

---

## 4. SFT 流水线

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `v9_expert_iter.sh` | **专家迭代模板**：采集策略自己的成功 → 与规划器示范混合 → 轻量 SFT → 门评 | 在不冒险的前提下提升现有策略。实测 +20 点，且**零风险**（最坏是没学到东西，不会像 PPO 那样打崩） |
| `v10_collect.sh` | 在指定区域采集当前策略的成功轨迹 | 专家迭代的第一步；也可单独用来**量策略在新区域的能力**（扩域前必做） |
| `v10_rest.sh` | 转换 → SFT → 环形门评 → 清理 | 接在生成之后，无人值守跑完剩余流程 |
| `v8_pipeline.sh` / `v4_pipeline.sh` / `v5_pipeline.sh` / `v3d_pipeline.sh` | 各版本完整流水线 | 复现历史；也是写新流水线的模板 |
| `v9_rest.sh` / `v3d_gate_resume.sh` | 从中途接管一条断掉的流水线 | 某段挂了但产物还在，不想从头再来。**`v9_rest.sh` 诞生于一次真实事故**：转换阶段的 `timeout` 会在离终点 25 分钟处杀掉它 |
| `v4b_verify.sh` | 对照实验：只换热启动权重，其余全同 | 验证复现文档里唯一未验证的那一步（从真机 SFT 热启动替代已废弃的血统） |
| `control_v4_rerun.sh` | 用旧检查点在**今天的环境**上重测 | 回答"基准本身有没有漂移"。不查这个，就会把环境变化误当成实验效应 |
| `v7_curriculum.sh` / `v7_orchestrator.sh` | 分区域课程式训练 | **已证伪**："去掉够不到的区域能抬高地板"这个假设不成立 |
| `overnight_v3.sh` / `overnight_v3b.sh` / `overnight_v3c.sh` / `sft_sim.sh` / `sft_pp.sh` / `phase2_pipeline.sh` | 早期流水线与单次 SFT | 历史 |

---

## 5. PPO

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `freeze_v11.sh` | **冻结探针**：跑真实训练路径但 `lr=1e-9`，一轮同时给出带噪与确定性两个成功率 | **PPO 的先决条件必须在 rollout 分布下测**，而不是确定性评测下。两者可差 50 点以上。这个探针 15 分钟给出答案，避免整夜白跑 |
| `rl_v13.sh` | **成功配方的启动器**，含自动停机守卫 | 守卫**必须写在启动器里**：写在会话里会随会话消失，历史上因此白烧 180 轮 |
| `ppo_night2.sh` | 探针编排器：按预先登记的判据自动扫参数并启动 PPO | 无人值守的整夜探索。**判据先写死**，避免看到数据再找理由 |
| `ppo_night.sh` | 探针编排器的第一版 | 它扫的是**空参数**（`noise_params` 对 `flow_noise` 无效），保留下来作为「调参之前先确认这个旋钮是否真的生效」的实例 |
| `noise_sweep.sh` | 噪声档位扫描 | 找"能保住 ≥5% 带噪成功率"的最高噪声。**注意**：本脚本扫的 `noise_params` 对 `flow_noise` 是**空参数**，正确的旋钮是 `noise_logvar_range` |
| `onlyeval_v11.sh` | 用 RL 配置只跑评测 | **已废弃的方法**：`only_eval` 同时切换模型规格来源并跳过训练环境创建，是另一条代码路径。用 `freeze_v11.sh` 代替 |
| `rl_v11.sh` | 官方配方原样的 PPO 启动器 | 复现"官方配方在本任务上失败"这个结果 |
| `rl_v6.sh` / `rl_sft_restart.sh` / `rl_resume750.sh` | 早期 PPO 启动器 | 历史 |

> **决定成败的三个参数**（详见 `PPO_V13_RUNBOOK_ZH.md`）：每轮更新次数 12→**1**（决定性）、`num_action_chunks` 5→**10**、`noise_logvar_range`→**[0.02,0.04]**。

---

## 6. 评测、验证与诊断

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `v13_verify.sh` | 诚实验证：**从未用过的种子** + 训练时的块长 | 门评种子上挑出来的成绩偏乐观（选择偏差）。诚实数字必须来自没参与挑选的局面 |
| `v13_baseline.sh` | 起点对照：同种子、同块长测起点 | 没有它，"PPO 涨了 N 点"只在门评口径上成立。**这是把"增益"与"评测集选择效应"分开的唯一办法** |
| `v8_verify.sh` | 独立验证评测：full clean + 超时 + 3 次重试 | 流水线里的评测被 Ray worker 猝死卡住过 24 分钟 |
| `bisect_v10.sh` | 同权重、同路径重测，确认"检查点/环境是否还是原来那个" | 出现"同一个权重在两处结果不同"时的**第一步**：先排除检查点和环境本身 |
| `bisect2.sh` | 逐项把 RL 特有的设置加进独立评测 | 二分定位差异项。**一次只加一个变量**，否则找到了也不知道是哪个 |
| `offline_replay_check.py` | **sim2real 的门**：用真机数据离线检验策略，与"仿真"和"完全不动"两个对照比 | 不碰机器人就能回答"策略在真实观测下还能不能输出合理动作"。实测比值 4.47（应 <1），挡住了一次会让机械臂乱动的部署 |
| `offline_check.sh` / `offline_check_nowrist.sh` | 上者的两个配置（双相机 / 只前视） | 检验"砍掉有问题的那一路输入能否救回来"。结果：不能（4.47→4.59），说明差距比单个相机缺陷更广 |
| `repro_eval.sh` / `dl_official_sft.sh` | 用**官方检查点**在**官方基准**上评测，对照论文数字 | 怀疑自己这套栈有系统性问题时**校准标尺**，不是比成绩 |
| `eval_pp.sh` / `eval_sft_fixed.sh` / `eval_simsft.sh` / `eval750_fixed.sh` | 各版本单次评测 | 手动评一个检查点 |
| `parse_eval_series.py` | 把一串评测日志解析成成绩序列 | 判断是在爬升还是衰减；**采收律**要求认出峰值 |

> **种子纪律**：门评种子（777/888）用来**挑**；诚实数字必须来自**从未参与挑选**的种子（用完作废）；采集种子单独一套（采过的局面已进训练集）。历史上门评与诚实值差 2–6 点。
> **判读纪律**：看**最好的检查点**，不是前两个。有一次前两个点是 7.8% 和 0.8%，最优点是 61.7%。

---

## 7. 监工与归档

| 脚本 | 干什么 | 要解决的问题 |
|---|---|---|
| `supervisor_v2.sh` | 训练监工：失败分类（shm/显存/端口/未知）后走对应诊断 | 无人值守时，**不同故障需要不同处置**；一律重启会掩盖真因 |
| `overnight_v6_supervisor.sh` | 早期整夜监工 | 历史 |
| `export_session_md.py` | 把整个会话导出成一个 Markdown（对话 + 文件 + 脚本） | 会话会被压缩，临时目录会消失。**归档必须连同索引一起做**，光复制文件不够 |
| `build_code_appendix.py` | 从活文件里抽取代码生成 runbook 附录 | 手抄的代码会与实际实现漂移 |
| `toolkits/preflight_config.py`（仓库主目录） | hydra 组装 + 校验 + 路径存在性 + **批量算术** | 每次启动训练前跑。它挡掉的是"跑一整夜才发现路径写错" |
| `toolkits/invariant_audit.py`（同上） | 9 项静默错误检查 | 换任务规格/数据集时。**第一次跑就查出 3 个真问题** |
| `toolkits/so101_to_usd.py`（同上） | 导出仿真场景与机器人为 USD | 与其它仿真器交换资产 |

---

## 8. 写新流水线时必须抄的六条

每一条都对应一次实际的整夜浪费：

1. **超时按实测速率定**，不要拍整数。转换 3.3 集/分 × 724 集 = 3h20m，写 3h 会在离终点 25 分钟处杀掉它。留 ≥50% 余量并把算术写进注释。
2. **等待哨兵**：发现上游进程消失后，**必须再查一次完成标记**才能判定失败——上游写标记和退出之间只差微秒。**别把哨兵字符串写进自己的日志文案**，否则后续 grep 会匹配到自己；**重跑一个曾失败过的阶段时，它自己的旧失败标记会立刻满足新的等待条件**。
3. **判进程存活别用会匹配到自身的 `pgrep -f`**（`pkill -f` 同理，会杀掉自己）。用启动时记下的 pid 读 `/proc/<pid>/stat` 状态位——setsid 下退出的 bash 会变成**僵尸**，`/proc/<pid>` 目录仍在。
4. **一个阶段结束时没有预先武装的后继 = 静默停摆**；而**多个后继不能并挂在同一个哨兵上**——它们会同时启动并互相杀 Ray（每个阶段的 `clean()` 都是全局的）。后继必须串成链。
5. **重试只对瞬态故障有用**。配置类错误（环境变量没导出、键不存在）重试三次只是把同一个错误重复三遍，还会把真正的错误信息埋在第三份日志里。
6. **清理规则要按实际压力触发**，不要无条件删。一次在 1.5 TB 空闲时删掉了非最优检查点，直接毁掉了"是否存在同时满足两个指标的检查点"这个最有价值的追问。

---

## 9. 相关文档

| 文档 | 内容 |
|---|---|
| `SO101_PIPELINE_ZH.md` | **端到端可复现流程**（从数据集到 PPO 产物）+ 摘要 + 局限 |
| `PPO_V13_RUNBOOK_ZH.md` | PPO 成功配方的详细步骤与失败对照表 |
| `PPO_CODE_WALKTHROUGH_ZH.md` | PPO 的代码级全流程（带文件行号） |
| `SIM2REAL_PLAN_ZH.md` | 真机部署方案与阻断项 |
| `V10_REPRODUCTION_ZH.md` | 阶段 A–E 的更详细版本 |
| `.claude/skills/rlinf-embodied-training/SKILL.md` | 工程纪律（含已证伪诊断清单） |
